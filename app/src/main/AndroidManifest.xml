<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a9kapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DisplayActivity"></activity>
        <activity android:name=".ControllStep4Activity" />
        <activity android:name=".ControllStep3Activity" />
        <activity android:name=".ControllStep2Activity" />
        <activity android:name=".ControllStep1Activity" />
        <activity
            android:name=".ValAvBussActivity"
            android:label="@string/title_activity_val_av_buss"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>